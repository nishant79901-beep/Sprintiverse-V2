import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{BrowserRouter,useNavigate,Routes,Route,Link}from'react-router-dom';import{createClient}from'@supabase/supabase-js';import{ArrowRight,Check,Inbox,Users,BarChart3,BookOpen,Settings,LogOut,Plus,Clock,Globe,UserRound,Activity,Mail,Plug,Forward,ShieldCheck,ChevronRight,Building2,CreditCard}from'lucide-react';import'./styles.css';
const SUPABASE_URL=import.meta.env.VITE_SUPABASE_URL||'https://gaukhsehjypoqdyepcnq.supabase.co';const SUPABASE_KEY=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY||'sb_publishable_KCIzzX0_71fn34GOgNignw_NtHZTrPm';const supabase=createClient(SUPABASE_URL,SUPABASE_KEY,{auth:{flowType:'pkce',detectSessionInUrl:true,persistSession:true,autoRefreshToken:true}});
const plans=[{id:'starter',name:'Starter',price:'₹1,599',usd:'$19',seats:2,tickets:500},{id:'growth',name:'Growth',price:'₹4,099',usd:'$49',seats:5,tickets:2500},{id:'pro',name:'Pro',price:'₹8,299',usd:'$99',seats:15,tickets:10000}];
function Public(){return <div className="public"><header><Link className="brand" to="/">◒ Sprintiverse</Link><nav><a href="#product">Product</a><a href="#pricing">Pricing</a><a href="#faq">FAQ</a></nav><div><Link className="linkbtn" to="/signin">Sign in</Link><Link className="btn" to="/signup">Start free</Link></div></header><main><section className="hero"><div className="eyebrow">CUSTOMER SUPPORT, ORGANIZED</div><h1>Turn every support request into a clear next step.</h1><p>One workspace for tickets, customers, conversations, and the people who resolve them.</p><div className="actions"><Link className="btn big" to="/signup">Start your 24-hour trial <ArrowRight size={17}/></Link><a className="ghost big" href="#product">See how it works</a></div><div className="mock"><div className="mockbar"><b>Inbox</b><span>12 open · 4 pending</span></div><div className="mockbody"><aside><span className="active">Inbox</span><span>Customers</span><span>Team</span><span>Knowledge</span></aside><div className="tickets"><div className="ticket"><i/><b>Question about my invoice</b><small>Aarav Mehta · High priority</small></div><div className="ticket"><i/><b>Unable to update account</b><small>Emily Carter · Waiting for reply</small></div><div className="ticket"><i/><b>Shipping address change</b><small>Michael Lee · Open</small></div></div></div></div></section><section id="product" className="section"><div className="sectionhead"><div className="eyebrow">THE WORKSPACE</div><h2>Everything your support team needs. Nothing they don't.</h2></div><div className="grid"><Feature icon={<Inbox/>} title="One support inbox" text="Keep every customer request visible, assigned, prioritized, and moving."/><Feature icon={<Users/>} title="Team collaboration" text="Give owners and agents clear responsibilities without losing context."/><Feature icon={<BarChart3/>} title="Useful visibility" text="See what is open, what is stuck, and where your team needs attention."/><Feature icon={<BookOpen/>} title="Knowledge & automation" text="Build repeatable answers and workflows as your support operation grows."/></div></section><section id="pricing" className="section pricing"><div className="sectionhead"><div className="eyebrow">PRICING</div><h2>Start small. Upgrade when support grows.</h2><p>Every plan is designed for a real support workspace.</p></div><div className="plans">{plans.map(p=><div className={'plan '+(p.id==='growth'?'popular':'')} key={p.id}>{p.id==='growth'&&<span className="badge">Most popular</span>}<h3>{p.name}</h3><div className="price">{p.price}<small>/month</small></div><div className="muted">{p.usd}/month · {p.seats} seats · {p.tickets.toLocaleString()} tickets</div><Link className="outline" to={'/signup?plan='+p.id}>Choose {p.name}</Link><ul><li><Check/> {p.seats} workspace seats</li><li><Check/> Up to {p.tickets.toLocaleString()} tickets/month</li><li><Check/> Core support workspace</li></ul></div>)}</div></section><section className="trial"><Clock/><div><b>Explore before you commit</b><span>New workspaces get a 24-hour trial with sample tickets so you can understand the product before paying.</span></div><Link className="btn" to="/signup">Try Sprintiverse</Link></section><section id="faq" className="section faq"><div className="sectionhead"><div className="eyebrow">FAQ</div><h2>Questions, answered.</h2></div><details open><summary>What is Sprintiverse?</summary><p>Sprintiverse is a customer-support workspace for teams that need a simple place to manage tickets, customers, conversations and agents.</p></details><details><summary>Can I try it first?</summary><p>Yes. New workspaces can skip billing and receive a 24-hour trial with sample data.</p></details><details><summary>Can I add agents?</summary><p>Owners can invite agents to their workspace during onboarding and manage the team later.</p></details></section></main><footer><span>◒ Sprintiverse</span><span>Customer support software for growing B2B teams.</span><span>© 2026 Sprintiverse</span></footer></div>}
function Feature({icon,title,text}){return <div className="feature"><div className="icon">{icon}</div><h3>{title}</h3><p>{text}</p></div>}
function Auth({signup=false}){const nav=useNavigate();const[loading,setLoading]=useState(false);const[msg,setMsg]=useState('');const[name,setName]=useState('');const[email,setEmail]=useState('');const[pw,setPw]=useState('');const google=async()=>{setLoading(true);setMsg('');const{error}=await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:`${location.origin}/auth/callback`,queryParams:{access_type:'offline',prompt:'select_account'}}});if(error)setMsg(error.message);setLoading(false)};const submit=async e=>{e.preventDefault();setLoading(true);setMsg('');if(signup){const{data,error}=await supabase.auth.signUp({email,password:pw,options:{data:{full_name:name},emailRedirectTo:`${location.origin}/auth/callback`}});if(error)setMsg(error.message);else if(data.session)nav('/onboarding');else setMsg('Check your email to confirm your account.')}else{const{error}=await supabase.auth.signInWithPassword({email,password:pw});if(error)setMsg(error.message);else nav('/app')}setLoading(false)};return <div className="auth"><Link className="brand" to="/">◒ Sprintiverse</Link><div className="authcard"><h1>{signup?'Create your workspace':'Welcome back'}</h1><p>{signup?'Start with Google or your work email.':'Sign in to your Sprintiverse workspace.'}</p><button className="google" onClick={google} disabled={loading}><Globe size={17}/> Continue with Google</button><div className="or"><span/>or<span/></div><form onSubmit={submit}>{signup&&<input required placeholder="Full name" value={name} onChange={e=>setName(e.target.value)}/>}<input required type="email" placeholder="Work email" value={email} onChange={e=>setEmail(e.target.value)}/><input required minLength={8} type="password" placeholder="Password (8+ characters)" value={pw} onChange={e=>setPw(e.target.value)}/><button className="btn full" disabled={loading}>{loading?'Please wait…':signup?'Create account':'Sign in'}</button></form>{msg&&<div className="notice">{msg}</div>}<small>{signup?'Already have an account? ':'New to Sprintiverse? '}<Link to={signup?'/signin':'/signup'}>{signup?'Sign in':'Create an account'}</Link></small></div></div>}
function AuthCallback(){const nav=useNavigate();const[error,setError]=useState('');useEffect(()=>{let active=true;let routed=false;const routeSession=async session=>{if(!active||routed||!session)return;routed=true;const{data:workspaces,error:workspaceError}=await supabase.from('workspaces').select('id').eq('owner_id',session.user.id).limit(1);if(!active)return;if(workspaceError){routed=false;setError(workspaceError.message);return}nav(workspaces?.length?'/app':'/onboarding',{replace:true})};const subscription=supabase.auth.onAuthStateChange((event,session)=>{if(session&&(event==='SIGNED_IN'||event==='INITIAL_SESSION'))routeSession(session)});supabase.auth.getSession().then(({data,error:sessionError})=>{if(sessionError&&active)setError(sessionError.message);else if(data.session)routeSession(data.session);else{const u=new URL(window.location.href);const oauthError=u.searchParams.get('error_description')||u.searchParams.get('error');if(oauthError&&active)setError(decodeURIComponent(oauthError.replace(/\+/g,' ')))}});return()=>{active=false;subscription.data.subscription.unsubscribe()}},[nav]);return error?<div className="loading"><div className="notice">{error}</div><Link className="btn" to="/signin">Return to sign in</Link></div>:<div className="loading">Completing secure sign-in…</div>}
function Onboarding(){const nav=useNavigate();const[session,setSession]=useState(null);const[name,setName]=useState('');const[role,setRole]=useState('owner');const[agents,setAgents]=useState(['']);const[loading,setLoading]=useState(false);useEffect(()=>{supabase.auth.getSession().then(({data})=>setSession(data.session))},[]);if(!session)return <Auth signup/>;const create=async()=>{setLoading(true);const{data,error}=await supabase.rpc('create_workspace_with_owner',{workspace_name:name,selected_currency:'INR'});if(error){alert(error.message);setLoading(false);return}const wid=data;if(role==='owner'){for(const e of agents.filter(Boolean))await supabase.from('workspace_invitations').insert({workspace_id:wid,email:e.toLowerCase(),role:'agent',invited_by:session.user.id})}sessionStorage.setItem('workspace_id',wid);nav('/billing')};return <div className="onboard"><div className="steps">01 Workspace <b>→</b> 02 Role <b>→</b> 03 Team</div><div className="oncard"><div className="eyebrow">WELCOME TO SPRINTIVERSE</div><h1>Set up your workspace</h1><p>A workspace is where your support team and customer conversations live.</p><input autoFocus placeholder="Workspace name" value={name} onChange={e=>setName(e.target.value)}/><label>Your role</label><div className="rolegrid">{['owner','agent'].map(r=><button className={role===r?'selected':''} onClick={()=>setRole(r)} key={r}><b>{r[0].toUpperCase()+r.slice(1)}</b><span>{r==='owner'?'Manage workspace, billing and agents':'Handle customer support tickets'}</span></button>)}</div>{role==='owner'&&<div className="agents"><label>Invite agents <small>optional</small></label>{agents.map((a,i)=><input key={i} type="email" placeholder="agent@company.com" value={a} onChange={e=>setAgents(x=>x.map((v,j)=>j===i?e.target.value:v))}/>)}<button className="ghost" onClick={()=>setAgents([...agents,''])}><Plus size={16}/> Add another agent</button></div>}<button className="btn full" disabled={!name.trim()||loading} onClick={create}>{loading?'Creating workspace…':'Continue to billing'}</button></div></div>}
function Billing(){const nav=useNavigate();const wid=sessionStorage.getItem('workspace_id');const[loading,setLoading]=useState(false);const[plan,setPlan]=useState('growth');const choose=async()=>{setLoading(true);const{error}=await supabase.from('workspaces').update({plan,billing_cycle:'monthly',currency:'INR',subscription_status:'pending'}).eq('id',wid);setLoading(false);if(error){alert(error.message);return}nav('/app')};const trial=async()=>{setLoading(true);await supabase.rpc('start_workspace_trial',{w:wid});await supabase.rpc('seed_sample_data',{w:wid});setLoading(false);nav('/app')};return <div className="billing"><div className="billhead"><Link className="brand" to="/">◒ Sprintiverse</Link><span>Workspace created</span></div><div className="billwrap"><div className="eyebrow">CHOOSE YOUR PATH</div><h1>Get your support workspace running.</h1><p>Choose a plan now, or explore Sprintiverse free for 24 hours with sample tickets.</p><div className="billplans">{plans.map(p=><button className={plan===p.id?'selected':''} onClick={()=>setPlan(p.id)} key={p.id}><b>{p.name}</b><strong>{p.price}<small>/mo</small></strong><span>{p.seats} seats · {p.tickets.toLocaleString()} tickets</span></button>)}</div><div className="billactions"><button className="ghost" onClick={trial} disabled={loading}>Skip & start 24-hour trial</button><button className="btn" onClick={choose} disabled={loading}>Continue with {plans.find(p=>p.id===plan).name} <ArrowRight size={16}/></button></div></div></div>}
function WorkspaceSettings({workspace,session,onLogout}){const[section,setSection]=useState('workspace');const[invite,setInvite]=useState('');const[status,setStatus]=useState('');const items=[['workspace','Workspace',Building2],['profile','Profile',UserRound],['performance','Agents performance',Activity],['invite','Invite agents',Mail],['integrations','Integrations',Plug],['forwarding','Email forwarding',Forward],['billing','Billing',CreditCard]];const sendInvite=async()=>{if(!invite.trim())return;const{error}=await supabase.from('workspace_invitations').insert({workspace_id:workspace?.id,email:invite.toLowerCase().trim(),role:'agent',invited_by:session.user.id});setStatus(error?error.message:'Invitation recorded. Email delivery will use the workspace sender.');if(!error)setInvite('')};return <div className="ownerpage"><div className="ownerhead"><div><div className="eyebrow">WORKSPACE</div><h1>Workspace settings</h1><p>Manage your workspace, team, performance and support integrations.</p></div><button className="ghost" onClick={onLogout}><LogOut size={16}/> Sign out</button></div><div className="settingslayout"><aside className="settingsmenu"><div className="settingsmenu-title">Workspace</div>{items.map(([id,label,Icon])=><button className={section===id?'active':''} onClick={()=>setSection(id)} key={id}><Icon size={17}/><span>{label}</span><ChevronRight size={14}/></button>)}</aside><section className="settingscontent">{section==='workspace'&&<><SettingsTitle icon={<Building2/>} title="Workspace" text="The identity and basic configuration of your support workspace."/><div className="settingscard"><label>Workspace name</label><div className="readonly">{workspace?.name||'Sprintiverse workspace'}</div><label>Owner</label><div className="personrow"><div className="avatar">{(session.user.user_metadata?.full_name||session.user.email||'O').slice(0,1).toUpperCase()}</div><div><b>{session.user.user_metadata?.full_name||'Workspace owner'}</b><span>{session.user.email}</span></div><em>Owner</em></div><label>Workspace ID</label><div className="readonly mono">{workspace?.id||session.user.id}</div></div></>}{section==='profile'&&<><SettingsTitle icon={<UserRound/>} title="Profile" text="Your identity inside Sprintiverse."/><div className="settingscard"><div className="personrow large"><div className="avatar">{(session.user.user_metadata?.full_name||session.user.email||'O').slice(0,1).toUpperCase()}</div><div><b>{session.user.user_metadata?.full_name||'Workspace owner'}</b><span>{session.user.email}</span></div></div><div className="profilegrid"><Field label="Name" value={session.user.user_metadata?.full_name||'Not set'}/><Field label="Role" value="Owner"/></div></div></>}{section==='performance'&&<><SettingsTitle icon={<Activity/>} title="Agents performance" text="Owner-level visibility into the people handling customer requests."/><div className="metricgrid"><Metric title="Active agents" value="0" note="Invite your first agent"/><Metric title="Open assignments" value="0" note="No agent workload yet"/><Metric title="Weekly resolution" value="—" note="Data will appear after activity"/></div><div className="settingscard emptysettings"><Activity/><b>Performance data will appear here</b><p>Once agents are invited and tickets are assigned, this area will show response time, resolution rate and workload.</p></div></>}{section==='invite'&&<><SettingsTitle icon={<Mail/>} title="Invite agents" text="Add the people who will work support tickets in this workspace."/><div className="settingscard"><label>Agent email</label><div className="invitefield"><input type="email" placeholder="agent@company.com" value={invite} onChange={e=>setInvite(e.target.value)}/><button className="btn" onClick={sendInvite}><Mail size={16}/> Send invite</button></div>{status&&<div className="notice">{status}</div>}<div className="infohint"><ShieldCheck size={17}/><span>Agents receive a separate role and cannot manage workspace billing or owner settings.</span></div></div></>}{section==='integrations'&&<><SettingsTitle icon={<Plug/>} title="Integrations" text="Connect the channels and tools that feed your support workspace."/><div className="integrationgrid"><Integration title="Email" text="Connect your support address and route customer replies into Inbox."/><Integration title="Razorpay" text="Billing integration for subscriptions and plan management."/><Integration title="Google" text="Authentication is connected and active." connected/></div></>}{section==='forwarding'&&<><SettingsTitle icon={<Forward/>} title="Email forwarding" text="Forward support messages to Sprintiverse and turn them into tickets."/><div className="settingscard forwarding"><div><span className="smalllabel">YOUR SUPPORT ADDRESS</span><strong>support@{(workspace?.name||'sprintiverse').toLowerCase().replace(/[^a-z0-9]+/g,'-')}.sprintiverse.pro</strong><p>Forward incoming customer email here. We will map replies to the correct conversation.</p></div><button className="ghost">Copy address</button></div><div className="settingscard"><b>How forwarding works</b><ol><li>Forward your support mailbox to the Sprintiverse address.</li><li>Sprintiverse creates a ticket from each new conversation.</li><li>Replies from agents stay attached to the same customer thread.</li></ol></div></>}{section==='billing'&&<><SettingsTitle icon={<CreditCard/>} title="Billing" text="Manage your Sprintiverse subscription and workspace limits."/><div className="settingscard billingstate"><div><span className="smalllabel">CURRENT PLAN</span><strong>24-hour trial</strong><p>Choose a paid plan when you're ready. Your workspace remains usable during the trial.</p></div><button className="btn" onClick={()=>location.href='/billing'}>View plans</button></div></>}</section></div></div>}
function SettingsTitle({icon,title,text}){return <div className="settingstitle"><div className="settingicon">{icon}</div><div><h2>{title}</h2><p>{text}</p></div></div>}
function Field({label,value}){return <div><span className="smalllabel">{label}</span><strong className="fieldvalue">{value}</strong></div>}
function Metric({title,value,note}){return <div className="metric"><span>{title}</span><strong>{value}</strong><small>{note}</small></div>}
function Integration({title,text,connected=false}){return <div className="integration"><div className="integrationicon"><Plug size={17}/></div><div><b>{title}</b><p>{text}</p></div><button className={connected?'connected':''}>{connected?'Connected':'Configure'}</button></div>}
function App(){const[session,setSession]=useState(null);const[loading,setLoading]=useState(true);const[tickets,setTickets]=useState([]);const[workspace,setWorkspace]=useState(null);const[page,setPage]=useState('inbox');const wid=sessionStorage.getItem('workspace_id');useEffect(()=>{supabase.auth.getSession().then(async({data})=>{setSession(data.session);if(data.session&&wid){const{data:t}=await supabase.from('tickets').select('*,customers(name,email)').eq('workspace_id',wid).order('last_activity_at',{ascending:false});setTickets(t||[]);const{data:w}=await supabase.from('workspaces').select('*').eq('id',wid).maybeSingle();setWorkspace(w||null)}setLoading(false)});},[]);if(loading)return <div className="loading">Loading Sprintiverse…</div>;if(!session)return <Auth/>;const logout=async()=>{await supabase.auth.signOut();sessionStorage.clear();location.href='/'};return <div className="app"><aside className="sidebar"><Link className="brand" to="/">◒ Sprintiverse</Link><div className="workspacechip"><div className="workspaceavatar">{(workspace?.name||'S').slice(0,1).toUpperCase()}</div><div><b>{workspace?.name||'Sprintiverse'}</b><span>Owner workspace</span></div></div><nav><button className={page==='inbox'?'navactive':''} onClick={()=>setPage('inbox')}><Inbox/> Inbox</button><button className={page==='customers'?'navactive':''} onClick={()=>setPage('customers')}><Users/> Customers</button><button className={page==='knowledge'?'navactive':''} onClick={()=>setPage('knowledge')}><BookOpen/> Knowledge</button><button className={page==='reports'?'navactive':''} onClick={()=>setPage('reports')}><BarChart3/> Reports</button></nav><div className="sidebottom"><button className={page==='settings'?'navactive':''} onClick={()=>setPage('settings')}><Settings/> Settings</button><button onClick={logout}><LogOut/> Sign out</button></div></aside>{page==='settings'?<WorkspaceSettings workspace={workspace} session={session} onLogout={logout}/>:<main className="dashboard"><header><div><div className="eyebrow">{page==='inbox'?'WORKSPACE':page.toUpperCase()}</div><h1>{page==='inbox'?'Inbox':page==='customers'?'Customers':page==='knowledge'?'Knowledge':'Reports'}</h1><p className="headcopy">{page==='inbox'?'Every support request, in one place.':page==='customers'?'Customer records and conversation history.':page==='knowledge'?'Reusable answers and internal support knowledge.':'Operational visibility for the workspace owner.'}</p></div><button className="btn"><Plus size={16}/> New ticket</button></header>{page==='inbox'?<><div className="stats"><div><b>{tickets.length}</b><span>Total visible tickets</span></div><div><b>{tickets.filter(t=>t.status==='open').length}</b><span>Open</span></div><div><b>{tickets.filter(t=>t.status==='pending').length}</b><span>Pending</span></div></div><div className="table"><div className="tablehead"><span>Ticket</span><span>Customer</span><span>Status</span><span>Priority</span></div>{tickets.length?tickets.map(t=><div className="tablerow" key={t.id}><b>#{t.number} · {t.subject}</b><span>{t.customers?.name||'Customer'}</span><span className={'status '+t.status}>{t.status}</span><span>{t.priority}</span></div>):<div className="empty"><Inbox size={30}/><b>Your inbox is ready</b><p>No tickets yet. Create your first ticket to begin.</p></div>}</div></>:<div className="placeholder"><div className="placeholdericon">{page==='customers'?<Users/>:page==='knowledge'?<BookOpen/>:<BarChart3/>}</div><h2>{page==='customers'?'Customers':page==='knowledge'?'Knowledge base':'Reports'}</h2><p>This owner workspace area is ready for the next Phase 1 build.</p></div>}</main>}</div>}
function Root(){return <Routes><Route path="/" element={<Public/>}/><Route path="/signin" element={<Auth/>}/><Route path="/signup" element={<Auth signup/>}/><Route path="/auth/callback" element={<AuthCallback/>}/><Route path="/onboarding" element={<Onboarding/>}/><Route path="/billing" element={<Billing/>}/><Route path="/app" element={<App/>}/></Routes>};createRoot(document.getElementById('root')).render(<BrowserRouter><Root/></BrowserRouter>);